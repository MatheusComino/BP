#include <bits/stdc++.h>

using namespace std;

int main(){

    float N, i, a, b, c, media;

    cout<<"Quantos casos voce vai digitar? "<<endl;
    cin>>N;

    for (int i = 0; i < N; i++)
    {
        cout<<"Digite tres numeros: "<<endl;
        cin>>a;
        cin>>b;
        cin>>c;
        media == (a * 2 + b * 3 + c * 5) / 10;
        cout<<"MEDIA = "<<media<<endl;
    }
}
