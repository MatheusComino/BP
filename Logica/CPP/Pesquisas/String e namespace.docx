-string

string é uma jeito de guardar um conjunto de char sem precisar criar um array manual de char;

é necessario colocar <string.h> para string funcionar.


- Principais funções string;

size(): retorna o tamanho corrente da string;

capacity(): retorna a capacidade corrente da string, ou seja, quantos elementos ela poderá conter antes de necessitar mais memória;

max_size(): retorna o tamanho máximo possível em uma string, geralmente dependente da máquina e do compilador.



-namespace

Por definição, a linguagem C++ utiliza o namespace std para definir todas as funções da biblioteca padrão.

Um namespace permite a definição de estruturas, estruturas, classes, funções, constantes, etc, que estarão vinculadas a ele.

Se não utilizarmos o comando using..., será necessário especificar explicitamente o namespace utilizado, como por exemplo